#include <iostream>
using namespace std;
double Opereic(double, double, double);
int main()
{
	setlocale(LC_ALL, "Russian");
	double y;

	double a;
	cin >> a;

	double a2;
	cin >> a2;

	double b2;
	cin >> b2;

	double c2;
	cin >> c2;

	y = Opereic(a2, c2, b2);
	cout << y << endl;
}


double Opereic(double a2, double c2, double b2)
{
	cout << endl << 180 << endl;

	if (a2 < 59 || a2 > 61)
	{
		cout << "Ошибка повторите ввод" << endl;
	}




	cout << endl << 180 << endl;

	if (b2 < 59 || b2 > 61)
		double b2; cin >> b2;
	{
		cout << "Ошибка повторите ввод" << endl;
	}




	cout << endl << 180 << endl;
	if (c2 < 59 || c2 > 61)
	{
		cout << "Ошибка повторите ввод" << endl;
	}
	double Opereic(double a, double c2, double b2);




	{
		double a = c2 / sin(b2);
		return a;
	}
}
