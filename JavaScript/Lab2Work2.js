'use strict';

let z 
let y = +prompt(("y = "));  
let a = +prompt(("a = ")); 
let e = +prompt(("e = ")); 
let x = +prompt(("x = ")); 
if (x * y > 0){ 
    z = 1 - Math.pow(e, x*y + a*b)  
} 
if ( (x * y) = 0 ){ 
    z = b - Math.min(a * x) 
}    
z = Math.min 
if (x * y< 0){ 
    z= Math.max(Math.pow(x, 3)) 
} 
console.log(z);
